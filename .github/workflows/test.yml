name: Scale Test

on: [push]

jobs:
  test:
    runs-on: [self-hosted, asg-runner]

    strategy:
      matrix:
        job: [1,2,3,4]

    steps:
      - run: echo "Job ${{ matrix.job }} running"
      - run: sleep 60
